/*using UnityEngine;

public class Card : MonoBehaviour
{
    private bool isUsed = false;

    public void UseCard()
    {
        if (!isUsed)
        {
            // Lakukan tindakan yang sesuai ketika kartu digunakan.
            // Misalnya, memindahkan kartu dari tangan ke deck Discard.
            isUsed = true;
            transform.SetParent(GameObject.Find("DiscardDeck").transform);
        }
    }
}
*/